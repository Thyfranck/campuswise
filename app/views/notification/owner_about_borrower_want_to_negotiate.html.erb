Dear <%= @user.name %>,
<br/>
<br/>
You wanted to <%= @exchange.package == "buy" ? "sell" : "lend" %> the book titled
'<%= @book.title %>' at the price $<%= @exchange.amount.to_f %>. But the
<%= @exchange.package == "buy" ? "buyer" : "borrower" %>
wants to <%= @exchange.package == "buy" ? "purchase" : "borrow" %>
 at the price $<%= @exchange.counter_offer.to_f %>.
<br/>
<br/>
To negotiate or accept or reject please login into our site <%= link_to "here", @url %>.
<br/>
<br/>
Campuswise.
