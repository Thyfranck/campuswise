<div class="span3"></div>
<div class="span5">

  <%= form_for @user, :url => create_phone_user_path(@user), :html => { :class => 'form-horizontal' } do |f| %>
    <fieldset>
      <legend>
        Phone Number
      </legend>

      <% if @user.errors.any? %>
        <div id="error_explanation" class="alert alert-error">
          <h4>
            Please correct following <%= pluralize(@user.errors.count, "field") %>:
          </h4>
          <ul>
            <% @user.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="control-group">
        <%= f.label :phone, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :phone, :class => 'text_field',:required => true %>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit "Submit", :class => 'btn btn-primary have_a_account' %>
      </div>
    <% end %>
  </fieldset>


</div>
<div class="span4"></div>